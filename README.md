# express-moviePassport
Express Movie App using Passport

# Config

Introduce `undefined` config values in `config-override.js` like the following:

    module.exports = { 
        apiKey: "apiKey",
        clientID: "clientID",
        clientSecret: "clientSecret"
    };
